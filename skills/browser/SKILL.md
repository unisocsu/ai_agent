# Browser

The browser capability is intentionally separated from the AI provider.

Current MVP supports the browser abstraction being added next. Do not claim DOM actions were performed unless a browser automation backend actually executed them.

Planned operations:
- open
- navigate
- back
- forward
- click
- type
- read
- screenshot
